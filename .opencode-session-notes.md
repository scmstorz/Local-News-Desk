# Session Notes

Stand: 2026-04-26

## Kontext

Lokale News-App in `/Users/storzs/Documents/scm/TECH/News-App`.

Wichtige Dateien:

- `local_news_backend.py`
- `local-news-app.html`
- `README_LOCAL.md`
- `LOCAL_ARCHITECTURE.md`
- `MODEL_OPTIMIZATION_GUIDE.md`
- `local-news.db`
- `local_config.json`

Die App laeuft lokal ueber:

```bash
.venv/bin/python app.py
```

Frontend danach im Browser ueber `http://127.0.0.1:8765/` nutzen. Direkte Nutzung von `local-news-app.html` bleibt als Fallback moeglich. Backend-Port laut Config: `127.0.0.1:8765`.

## Aktueller Dirty State

Uncommitted Aenderungen bestehen in:

- `local_news_backend.py`
- `local-news-app.html`
- `README_LOCAL.md`
- `LOCAL_ARCHITECTURE.md`
- `MODEL_OPTIMIZATION_GUIDE.md`

Diese Aenderungen wurden bewusst nicht zurueckgesetzt.

## Gefixte Punkte

### 1. Embedding-Fehler bei Unicode-Titeln

Problem:

```text
Embedding generation failed for article 18701: Ollama embedding response did not contain a usable vector
```

Ursache:

- Artikel `18701` hatte kyrillischen/Unicode-Titel.
- `build_embedding_input_text()` nutzte vorher die ASCII-Titelnormalisierung.
- Ergebnis war leerer String `''`.
- Ollama bekam leeren Input und lieferte keinen nutzbaren Vector.

Fix:

- `build_embedding_input_text()` hat jetzt Unicode-freundlichen Fallback.
- Leere Embedding-Inputs werden in `select_article_for_embedding()` uebersprungen.
- Ollama-Embedding-Response-Parser akzeptiert jetzt:
  - `embedding: [...]`
  - `embeddings: [[...]]`
  - `embeddings: [...]`
  - `data[0].embedding`

Verifikation:

- Artikel `18701` erzeugte danach einen echten Input:

```text
'Сэм Альтман: «Хатоларимиз учун аҳолидан чуқур узр сўрайман»'
```

- In der DB wurde fuer `18701` ein Embedding gespeichert:

```text
18701 | nomic-embed-text-v2-moe:latest | length 10277 | 2026-04-26T10:50:10Z
```

### 2. Summary nach `s` im Feed

Problem:

- User drueckte `s` im empfohlenen Feed und hatte den Eindruck, dass keine Summaries entstehen.
- DB zeigte: Artikel `18556` wurde requested, processing, dann ready.
- Also war API grundsaetzlich aktiv, aber Summary-Arbeit konnte durch Embedding-Worker/Ollama-Lock verzoegert wirken.

Fix:

- `RuntimeState` hat jetzt `summary_event`.
- `set_feed_decision(..., "summarize")` ruft nach DB-Update `wake_summary_worker()`.
- `summary_worker()` wartet auf Event statt nur Polling.
- `embedding_worker()` pausiert, sobald Summary-Arbeit `queued` oder `processing` ist.
- `/api/articles/<id>/summarize` behandelt `RuntimeError` jetzt sauber als HTTP 409.

Verifikation:

- Artikel `18556` ist ready/unreviewed:

```text
SpaceX und OpenAI planen IPOs – Historie mahnt zur Kursvorsicht
```

### 3. `Empfohlen` und `Vielleicht` zeigten doppelte Artikel

Problem:

- User sah wahrscheinlich dieselben Artikel in `Empfohlen` und danach in `Vielleicht`.
- Ursache war UI-Definition: Modus hiess `Vielleicht+` und zeigte bewusst `Empfohlen + Vielleicht`.

Fix:

- Frontend-Button heisst jetzt `Vielleicht`.
- Frontend filtert `Vielleicht` nur noch mit `prediction.tier === "maybe"`.
- Backend unterstuetzt `mode=maybe`.
- `maybe_plus` bleibt backendseitig als Kompatibilitaetsmodus vorhanden.

Verifikation mit aktuellem Feed:

```text
recommended 14
maybe 35
overlap 0
```

Zusaetzlich wurde der sichtbare Feed auf exakte/nahe Titelduplikate geprueft:

```text
exact-normalized-dupes 0
near pairs 0
```

### 4. `S`-Taste im Feed kam nicht mehr zuverlaessig an

Problem:

- User meldete, dass mehrfaches Druecken von `S` im `Empfohlen`-Feed nichts ausloest.
- DB bestaetigte: Nach `2026-04-26T10:59:00Z` gab es weitere `article_skipped`-Events, aber keine neuen `summary_requested`-Events.
- Damit war klar: Backend/Worker waren nicht der primaere Fehler; der Tastendruck kam im Frontend nicht robust im `handleFeedSummarize()`-Pfad an.

Fix:

- Keyboard-Listener laeuft jetzt in der Capture-Phase:

```js
document.addEventListener('keydown', handleKeyboard, true);
```

- Shortcut-Erkennung akzeptiert nicht nur `event.key === "s"`, sondern auch die physische Taste `event.code === "KeyS"`.
- Behandelte Shortcut-Events rufen `preventDefault()` und `stopPropagation()` auf.
- Editable Targets (`input`, `textarea`, `select`, `contenteditable`) werden ausgenommen.
- `event.repeat` wird ignoriert, damit gehaltene Tasten nicht mehrere Artikel versehentlich wegschieben.
- Beim Druecken von `S` passiert sofort sichtbar etwas:
  - Toast `Summary vorgemerkt.`
  - Feed-Karte wird sofort entfernt bzw. Empty-State gerendert
  - sichtbarer Status `Summary vorgemerkt: ...`
- Wenn kein aktiver Feed-Artikel vorhanden ist, gibt es jetzt einen Toast statt stillem Return.

Wichtig:

- User muss `local-news-app.html` im Browser hart neu laden, damit der neue Shortcut-Code aktiv ist.

## Pruefungen, die sauber waren

```bash
PYTHONPYCACHEPREFIX=/tmp/pycache python3 -m py_compile app.py local_news_backend.py
perl -0ne 'while (m{<script>(.*?)</script>}sg) { print $1 }' local-news-app.html | node --check
git diff --check
```

## Mini-Regressionssuite

Nach Commit `20f116a` wurde als separater Stabilisierungsschritt eine kleine
`unittest`-Suite angelegt:

- Datei: `tests/test_regressions.py`
- UI-HTTP-Vertrag: `tests/test_ui_http_contract.py`
- Ausfuehren mit:

```bash
.venv/bin/python -m unittest discover -s tests -v
```

Arbeitsregel ab jetzt:

- Nach jeder Codeaenderung an Backend, Frontend oder Datenmodell muss diese Suite laufen.
- Bei jedem Bugfix moeglichst einen passenden Regressionstest ergaenzen.
- Vor einer finalen Rueckmeldung mindestens ausfuehren:

```bash
.venv/bin/python -m unittest discover -s tests -v
PYTHONPYCACHEPREFIX=/tmp/pycache python3 -m py_compile app.py local_news_backend.py tests/test_regressions.py tests/test_ui_http_contract.py
perl -0ne 'while (m{<script>(.*?)</script>}sg) { print $1 }' local-news-app.html | node --check
git diff --check
```

Aktuelle Abdeckung:

- `/api/articles/<id>/summarize` setzt `feed_decision = summarize`, `summary_status = queued`, loggt `summary_requested` und weckt den Summary-Worker
- `/api/articles/<id>/skip` setzt `feed_decision = skip` und loggt `article_skipped`
- `/api/feed?mode=recommended` und `/api/feed?mode=maybe` sind disjunkt
- Unicode-Titel bleiben als Embedding-Input erhalten
- Ollama-Embedding-Parser akzeptiert mehrere Response-Formate
- `summary_work_pending()` erkennt `queued` und `processing`
- Summary-Statusuebergaenge sind abgedeckt: Failed-Requeue, Skip-Konflikt, queued -> processing -> ready, failed und stale-processing recovery
- Summary-Queue/Worker wurde weiter ausgeduennt: `select_next_queued_summary_job()` und `mark_summary_failed()` kapseln Queue-Auswahl und Failed-Transition separat und sind direkt getestet.
- Kurze/leere/fehlgeschlagene Volltext-Extraktion fuehrt nicht mehr automatisch zu `summary_status = failed`; stattdessen wird ein konservativer Summary-Input aus Titel, Quelle, URL und kurzem Textauszug gebaut. Diese Ausgabe ist keine echte Volltext-Summary und wird mit `summary_is_fallback` in DB/API/UI sichtbar als solche markiert; bestehende Events mit `extraction_fallback = true` werden migriert.
- Summary-Fetch nutzt mehrere URL-Kandidaten: decodierte `link_to_article`, originaler RSS-Link und nicht-homepage-artige `source_url`; reine Publisher-Homepages werden nicht als Artikel-URL missbraucht
- Embedding-Worker-Prioritaet ist ueber `run_embedding_worker_once()` abgedeckt: bei Summary-Arbeit wird kein Embedding-Job selektiert
- Embedding-Auswahl ist abgedeckt: leere Inputs werden uebersprungen, passende Embeddings nicht neu selektiert, geaenderter Hash/Modellwechsel neu selektiert, Prioritaet pending -> summarize -> skip, 72h-Fenster
- Embedding-Kandidatenauswahl ist als `select_embedding_candidate()` von der DB-Abfrage getrennt und direkt getestet.
- Ollama-Embedding-Aufruf ist weiter gekapselt: `normalize_embedding_text()` und `build_ollama_embedding_request_specs()` trennen Input-Normalisierung/Request-Formate vom HTTP-Aufruf; der Fallback von `/api/embed` auf `/api/embeddings` ist getestet.
- Ollama-Summary-Aufruf ist weiter gekapselt: `build_summary_prompt()` und `build_ollama_summary_payload()` trennen Prompt-/Payload-Bau vom HTTP-Aufruf; Payload und Parsing sind getestet.
- Event-Payloads sind ueber `encode_event_payload()` und `decode_event_payload()` gekapselt; Logging, Auswertung und Tests nutzen dieselbe robuste JSON-Schicht.
- `app_state` ist ueber `encode_app_state_value()`, `decode_app_state_value()`, `upsert_app_state()` und `fetch_app_state()` gekapselt; Codec und Store-Verhalten sind getestet.
- Artikel-Lookup per ID ist ueber `fetch_article_by_id()` gekapselt und wird in Feed-/Summary-Feedback-Flows genutzt.
- Artikel-Event-Abfragen sind ueber `fetch_article_events()` und `fetch_recent_article_events()` gekapselt; Tests und Feed-Prediction-Stats nutzen diese Helper.
- Feed-Entscheidungen sind weiter gekapselt: `build_feed_decision_transition()` berechnet Status/Event-Regeln, `apply_feed_decision_transition()` schreibt die DB-Aenderung; Endpoint-Flows bleiben durch Tests abgesichert.
- Summary-Feedback ist weiter gekapselt: `validate_summary_feedback()` und `apply_summary_feedback()` trennen Validierung/DB-Update/Event-Logging vom API-Orchestrator.
- Summary-Review-Abfragen sind weiter gekapselt: `is_reviewable_summary_status()`, `fetch_review_summary_rows()` und `fetch_summary_counts()` definieren Review-Queue und Counts zentral; Counts laufen ueber eine Aggregatquery.
- API-Fehlerantworten fuer Feed-Aktionen und Summary-Feedback sind ueber `api_error_response()`, `run_feed_action_api()` und `run_summary_feedback_api()` zentralisiert; Routen sind duenner.
- Embedding-Vektor-Speicherung ist ueber `encode_embedding_vector()` und `decode_embedding_vector()` gekapselt; Laden ueberspringt defekte DB-Werte, Speichern validiert finite numerische Vektoren.
- Generische DB-JSON-Felder sind ueber `decode_json_object()`/`decode_json_array()` gekapselt; Compare-Model-Listen und Model-Run-JSON-Felder nutzen robuste Decoder.
- Model-Ops-Payload-Bau ist aus der Route extrahiert: `build_model_ops_target_payload()` und `build_model_ops_payload()` kapseln Target-Anreicherung und Gesamtresponse.
- Feed-API-Payload-Bau ist aus der Route extrahiert: `build_feed_similarity_counts()`, `load_feed_rows_for_api()` und `build_feed_api_payload()` kapseln Snapshot-Fallback, Prediction, Cache-Update, Filterung und Serialisierung.
- DB-Event-Logging ist mit Payloads abgedeckt fuer `summary_requested`, `article_skipped`, `summary_processing_started`, `summary_generated`, `summary_failed`, `summary_processing_recovered` und `summary_feedback`
- Frontend enthaelt den robusten `S`-Shortcut in der Capture-Phase
- Backend liefert `local-news-app.html` ueber `/` und `/app` aus
- Frontend nutzt bei HTTP-Auslieferung `window.location.origin + "/api"` und bei direktem Datei-Oeffnen weiter `http://127.0.0.1:8765/api`

Hinweis:

- User hat am Ende der Session Playwright-MCP fuer Codex in `~/.codex/config.toml` eingetragen:

```toml
[mcp_servers.playwright]
command = "npx"
args = ["@playwright/mcp@latest"]
```

- Playwright-MCP ist in dieser Session als Tool sichtbar und funktioniert grundsaetzlich, blockiert aber `file://`-URLs. Der selbstgebaute Chrome/CDP-Test wurde deshalb durch `tests/test_ui_http_contract.py` ersetzt; echte Browserchecks sollten die App kuenftig ueber HTTP testen.

Letzter Lauf:

```text
Ran 74 tests in 0.864s
OK
```

API-Smoke per Flask-Testclient:

```text
/api/status 200
/api/feed 200
/api/summaries 200
```

## Offene Hinweise fuer naechste Session

- Backend nach Codeaenderungen neu starten:

```bash
.venv/bin/python app.py
```

- Browserseite hart neu laden. Empfohlen ist jetzt `http://127.0.0.1:8765/`; direktes Oeffnen von `local-news-app.html` bleibt als Fallback moeglich.
- Danach im Feed pruefen:
  - `Empfohlen` durcharbeiten
  - `Vielleicht` durcharbeiten
  - keine Ueberschneidung mehr erwartet
  - `s` sollte Summary sofort queuen; Summaries haben Vorrang vor Embeddings

## Big-Picture-Refactor-Plan

Konservativ in kleinen, getesteten Schritten weiterarbeiten:

1. Summary-Queue/Worker isolieren: Statusuebergaenge, `summary_event`, `summary_work_pending`, Worker-Prioritaet und API-Queueing klarer buendeln.
2. Embedding/Ollama-Code trennen: Embedding-Input, Response-Parsing, Ollama-Aufrufe und Worker-Pausierung fachlich zusammenfassen.
3. DB-Zugriffe kapseln: kleine Helper fuer Artikelstatus, Events, Feed-Queries und Summary-Queries schaffen; keine grosse ORM-Schicht.
4. Flask-Routes duenner machen: Routen sollen Request lesen, Service-Funktion aufrufen und JSON zurueckgeben.
5. Frontend-State-Logik ausduennen: Feed-Modus-Filter, lokale Counts, Mutation-Queue und Shortcut-Handling in klare Helper verschieben.
6. Testqualitaet verbessern: String-/Vertragstests behalten, aber mindestens ein verlaessliches echtes Browser-Szenario fuer Feed-Aktionen aufbauen.

Leitregel:

- Erst Sicherheitsnetz stabilisieren, dann Code verschieben.
- Verhalten pro Schritt moeglichst nicht aendern.
- Nach jedem Schritt Suite und Checks ausfuehren.
- Bei jedem Bugfix einen gezielten Regressionstest ergaenzen.

## Geplanter Testausbau

Gezielt testen, bevor die naechsten fachlichen Bloecke verschoben werden:

1. Erledigt: Summary-Statusuebergaenge: Queueing, Processing, Ready/Failed, Fehlerantworten und Retry-/Konfliktverhalten.
2. Erledigt: Worker-Prioritaet: Embedding-Arbeit muss pausieren, solange Summary-Arbeit `queued` oder `processing` ist.
3. Echter Browser-Flow fuer Feed-Aktionen: API stubben, `Empfohlen` leer klicken, lokaler Wechsel auf `Alle` ohne Feed-Reload.
4. Erledigt: DB-Event-Logging: zentrale Aktionen schreiben die erwarteten Events und Payloads (`skip`, `summarize`, Summary-Processing, Feedback).
5. Erledigt: Embedding-Auswahl: leere Inputs, bereits eingebettete Artikel, Hash-/Modellwechsel, Prioritaet und 72h-Fenster.

## Commit-Vorschlag

Wenn die App nach manuellem Test passt:

```bash
git add local_news_backend.py local-news-app.html README_LOCAL.md LOCAL_ARCHITECTURE.md MODEL_OPTIMIZATION_GUIDE.md tests/test_regressions.py tests/test_ui_http_contract.py .opencode-session-notes.md
git commit -m "Stabilize local news regressions and UI serving"
```
